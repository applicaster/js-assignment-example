// for quick dev, you can define your access keys here, but we'd rather not see them
// committed on github

module.exports = {
  UNSPLASH_ACCESS_KEY: process.env.UNSPLASH_ACCESS_KEY || "",
  UNSPLASH_SECRET_KEY: process.env.UNSPLASH_SECRET_KEY || ""
};
